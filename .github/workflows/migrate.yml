name: Migrate all records of an event from v1 to v2

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to run migration in'
        required: true
        type: choice
        default: 'development'
        options:
          - staging
          - development
          - qa
          - datamigration
      event:
        description: 'The event to process (birth or death)'
        required: true
        type: string
      domain:
        description: 'The environment domain (without protocol or subdomains)'
        required: true
        type: string
        default: 'dev.crvs.gov.to'
      client_id:
        description: 'System Client ID'
        required: false
        type: string
      client_secret:
        description: 'System Client Secret'
        required: false
        type: string
      skip:
        description: 'Skip count from failed run (use "Resume with skip" value to start after the error record)'
        required: false
        type: number
        default: 0

jobs:
  fetch-and-transform:
    timeout-minutes: 7200
    name: Migrate ${{ github.event.inputs.event }} in ${{ github.event.inputs.environment }} (${{ github.event.inputs.domain }})
    runs-on: [self-hosted, linux, "${{ github.event.inputs.environment }}"]
    environment: ${{ github.event.inputs.environment }}
    outputs:
      script_output: ${{ steps.deno-script.outputs.script_output }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          ref: ${{ github.ref }}

      - name: Install dependencies
        run: |
          if ! command -v jq &> /dev/null; then
            echo "jq not found. Installing..."
            sudo apt-get update
            sudo apt-get install -y jq
          else
            echo "jq is already installed."
          fi

          if ! command -v deno &> /dev/null; then
            echo "deno not found. Installing..."
            curl -fsSL https://deno.land/install.sh | sh
            echo "$HOME/.deno/bin" >> $GITHUB_PATH
          else
            echo "deno is already installed."
          fi

      - name: Convert notebook to TypeScript
        run: |
          mkdir -p tmp
          bash ./scripts/deno-notebook-to-deno.sh ./v1-to-v2-data-migration/migrate.ipynb ./tmp/migrate.ts

      - name: Run the script with Deno
        id: deno-script
        env:
          OPENCRVS_EVENT: ${{ github.event.inputs.event }}
          OPENCRVS_DOMAIN: ${{ github.event.inputs.domain }}
          RECORD_SKIP: ${{ github.event.inputs.skip }}
          SEQUENCE_SQLITE_PATH: ./v1-to-v2-data-migration/data/seq.db
          MIGRATION_SUMMARY_PATH: ./tmp/migration-summary.txt
          MIGRATION_FAILURES_HISTORY_PATH: ./tmp/migration-failures.jsonl
          MIGRATION_REG_CHANGES_HISTORY_PATH: ./tmp/migration-reg-number-changes.jsonl
          OPENCRVS_CLIENT_ID: ${{ github.event.inputs.client_id }}
          OPENCRVS_CLIENT_SECRET: ${{ github.event.inputs.client_secret }}
        run: |
          set +e
          deno run --allow-net --allow-env --allow-read --allow-write --allow-ffi ./tmp/migrate.ts 2>&1 | tee /tmp/migrate-output.txt
          EXIT_CODE=${PIPESTATUS[0]}
          {
            echo "script_output<<MIGRATE_OUTPUT_EOF"
            cat /tmp/migrate-output.txt
            echo "MIGRATE_OUTPUT_EOF"
          } >> $GITHUB_OUTPUT
          exit $EXIT_CODE

      - name: Upload migration artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: migration-${{ github.event.inputs.event }}-${{ github.run_id }}
          path: |
            tmp/migration-summary.txt
            tmp/migration-failures.jsonl
            tmp/migration-reg-number-changes.jsonl
            v1-to-v2-data-migration/data/seq.db
          retention-days: 90
          if-no-files-found: warn

      - name: Generate summary
        run: |
          echo "## Notebook result" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "${{ steps.deno-script.outputs.script_output }}" >> $GITHUB_STEP_SUMMARY
